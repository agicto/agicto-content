export const metadata = {
  title: '在 langchain 中使用 DevAGI',
  description:
    'On this page, we’ll dive into the different message endpoints you can use to manage messages programmatically.',
}

# 配置 OpenAI 环境变量

> 已经获得了我们通过 DevAGI 中赠送的 API Key


## 在当前文件夹里面建一个名为 .env 的文件，内容如下

<CodeGroup>

```bash {{ language: 'bash' }}
OPENAI_API_KEY=sk-xxx
OPENAI_API_BASE=https://api.agicto.cn/v1
```
</CodeGroup>


## 在自己的项目文件夹下，安装项目依赖的包
<CodeGroup>

```bash {{ language: 'bash' }}
pip install python-dotenv langchain-openai
```
</CodeGroup>



## 新建 index.py 文件

<CodeGroup>

```python {{ language: 'python' }}
from langchain_openai import ChatOpenAI
from dotenv import load_dotenv, find_dotenv

_ = load_dotenv(find_dotenv())
llm = ChatOpenAI(model="gpt-3.5-turbo") # 默认是gpt-3.5-turbo
response = llm.invoke("讲个笑话")
print(response.content)
```
</CodeGroup>