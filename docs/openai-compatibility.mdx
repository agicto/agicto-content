export const metadata = {
  title: 'openai 兼容性',
  description:
    'This guide will get you all set up and ready to use the Protocol API. We’ll cover how to get started an API client and how to make your first API request.',
}

<Note>
  AGICTO API 提供对 OpenAI API 标准的兼容性，更轻松地集成到现有应用程序中。
</Note>

# 案例：使用 gemma


<CodeGroup>

```python {{ language: 'python' }}
from openai import OpenAI

client = OpenAI(
    api_key = "自己的API key",
    base_url = "https://api.agicto.cn/v1"
)

chat_completion = client.chat.completions.create(
    messages=[
        {
            "role": "user",
            "content": "讲个笑话",
        }
    ],
    model="google/gemma-7b-it", # 此处更换其它模型,请参考模型列表 eg: google/gemma-7b-it
)

print(chat_completion.choices[0].message.content)

```
</CodeGroup>

